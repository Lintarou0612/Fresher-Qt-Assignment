function retranslateUi(ctext) {
    if( ctext === "en")
        translator.setTranslation("en")
    if(ctext === "kor")
        translator.setTranslation("kor")
    if(ctext === "vi")
        translator.setTranslation("vi")
}
